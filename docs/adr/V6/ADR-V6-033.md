# ADR-V6-033：spec 文档诚实化（§4.2/§7.3 实际落点 + §2.1 叙事 + ADR-022 C7 标签纠正）

- **状态**：Accepted（Phase 1b 收口·批次 3，2026-07-20）
- **日期**：2026-07-20
- **Phase**：1b（诚实化）
- **关联**：ADR-V6-022（母纲，桶 C item C7 / action 17+18）、ADR-V6-029 D4（§2.1 已商定措辞）、ADR-V6-032（Quark/Theory 契约，§7.3 落点引用）、ADR-V6-031（§4.3C 同族「过度承诺在 V5 doc 非 fork」先例）
- **铁律**：C1（本决策一 ADR）、C3（生产现实即真理——文档须对齐 fork 实代码非反之）

---

## 背景

ADR-V6-022 审计桶 C item C7 / action 17+18：

- **action 17**：「§7.3 命名契约（12 realityos_* 只 3 个在）—— P2 文档对齐（加「实际落点」列）」
- **action 18**：「§2.1「6 messenger 内置生产级」叙事修正（3 built-in + 3 plugin，非 6 built-in）」

母纲这两条指向设计总纲 `/Users/wugang/danao13/RealityOS-V6-架构设计.md`。

## 亲核调查结论（反假绿，Explore 24 tool-use 双路并行核）

调查推翻了母纲 C7 行的**字面措辞**（C7 行本身有事实偏差，须纠正）：

1. **「命名契约」非 doc 原文**：全仓搜 `命名契约` 在设计总纲**零命中**——它是 ADR-V6-022 贴的标签。doc §7.3 实际标题是「**NEW + BORROW**」，列的是 **15 个 V6 模块**（`realityos_ptg`/`realityos_atomizer`/…），**不是 12 张表**。
2. **「12 表」在 §4.2 非 §7.3**：「PTG 完整 Schema」12+ 表在 §4.2（line 262），且这些表**不带 `realityos_` 前缀**（用 `memos`/`identity_events` 等裸名）。
3. **fork `realityos_` 前缀表仅 1 张**（`realityos_users`），非 3 非 12。母纲「只 3 个在」的「3」实指 §7.3 的 3 个**模块名**落地为插件目录（`realityos_sovereignty`/`realityos_security`/`realityos_insights`），非表。
4. **§2.1/§4.2/§7.3 过度承诺全部只在 danao13 doc**：fork 内零携带（搜 `内置生产级`/`命名契约`/§7.3 内容在 fork 仅命中审计 ADR 自身）。**fork 分发物本就诚实**（ADR-031 同族先例：§4.3C 过度承诺在 V5 doc 非 fork）。

故本 ADR 的 fork 侧动作 = **纠正母纲 C7 标签 + 记录 danao13 doc 对齐动作**；fork 源码零改动（已诚实）。

## 决策

### D1：纠正 ADR-V6-022 桶 C item C7 标签（inline correction）

母纲 C7 行 `§7.3 命名契约（12 realityos_* 只 3 个在）` 三处偏差：(a) §7.3 实为「NEW + BORROW」15 模块非 12 表；(b) 「12 表」是 §4.2；(c) 「3 在」指模块非表，fork `realityos_` 表仅 1 张。在 ADR-022 C7 行加 REV-10 纠正注（保留原文备查，非删改决策史）。

### D2：danao13 doc §4.2 加「实际落点」对账（REV-10）

在 `/Users/wugang/danao13/RealityOS-V6-架构设计.md` §4.2 schema 块后插「REV-10 实际落点对账」注（对账 fork `plugins/memory/ptg/schema.py` SCHEMA_VERSION=6 真相）：
- **9 表同名落地**：memos/identity_events/meaning_events/feeling_events/entity_events/entities/relations/insight_aggregation/dlq_messages。
- **1 表改名**：`task_queue`（doc）→ `task_suggestions`（fork，V 域提醒语义收窄）。
- **6 表未建（诚实，各有理由）**：raw_audio_files/audio_clips（降为列承载）/time_trend_daily（Phase 2 特性，ADR-022 D1#9 降级）/schema_errors（DLQ 统一承载）/location_log/device_signal_log（V6 砍 GPS/传感器，doc 自标预留不建）。
- **fork 新增 6 表（doc §4.2 未列）**：realityos_users/feedback/quality_metrics/tool_events（含 quark_evidence 预留列）/llm_call_logs（C6）/ptg_meta。

### D3：danao13 doc §7.3 加「实际落点」对账（REV-10）

在 §7.3 模块表后插「REV-10 实际落点对账」注（15 模块对账 fork HEAD 63bcc8ecd）：
- 3 独立插件目录（sovereignty/security/insights）+ 4 并入 PTG 大脑（ptg/atomizer/confidence/capture）+ 质量/灾备/校准 3 实现 + 4 契约定死或 Phase 2 待启（quark/theory IDL 已钉 ADR-032；orchestrator/rag 未启）+ 1 诚实降级（acoustic Phase 2.5，ADR-031）+ 2 未启（commitments/薄客户端）。无孤儿。
- fork 新增 doc §7.3 未列插件 `realityos_calibration`（ADR-028，承载 `realityos_quality` 行的校准回流职责）。

### D4：danao13 doc §2.1 叙事修正（action 18，落地 ADR-029 D4 已商定措辞）

§2.1 line 138 `全部 6 个内置、生产级（原生语音/图片）` 改为：6 核心内置 adapter（telegram/discord/slack/whatsapp/feishu/dingtalk，Platform enum 成员）+ ~14 平台插件；「内置」≠「生产级」（生产级须过 L2 桶 D 活验）；出站语音现状仅 telegram/feishu/wecom，dingtalk 受 webhook API 限、yuanbao 未实现（详见 ADR-V6-034）。

## 不做（诚实）

- **不改 fork 源码**：fork 本就诚实（§2.1/§4.2/§7.3 内容 fork 零携带）。本 ADR fork 侧仅文档（本 ADR + ADR-022 inline 纠正）。
- **不把 danao13 doc 迁入 fork**：设计总纲是 V5 仓库工作文档（untracked），非 fork 分发物；迁入是独立仓库组织决策，超本批范围。本 ADR 只对齐其内容使 spec 不再撒谎。
- **不补建 6 张未建表**：raw_audio_files/audio_clips/time_trend_daily 等各有 Phase/降级理由（详见 D2），非漏做；Phase 2 起按需建。

## 后果

- ADR-V6-022 桶 C item C7（§7.3 命名契约）+ action 18（§2.1 叙事）**收口**：spec doc 从「声称 N 表/N 模块/N 内置」升级为「逐条对账 fork 实代码落点」。
- 母纲 C7 行事实偏差纠正（§7.3=15 模块非 12 表；表在 §4.2；realityos_ 表仅 1），后续读者不被误导。
- 反假绿：spec doc 停止过度承诺；fork 分发物本就诚实的状态被显式记录（非靠「doc 在别处」搪塞）。

## 验证

- danao13 doc §2.1/§4.2/§7.3 三处 REV-10 对账注已落地（grep `REV-10 实际落点对账` / `ADR-V6-033` 命中）。
- ADR-022 C7 行 inline 纠正注已加。
- 本 ADR + ADR-022 纠正为 fork 文档提交，三连核验：headSha == CI 绿 + 内容指纹。
