# ADR-V6-053 — 五份审计综合 + ADR-V6-040 收口后统一规划（诚实口径校准）

**状态**: Accepted
**日期**: 2026-07-20
**关联**: [ADR-V6-040](ADR-V6-040.md)（统一计划，子集 100%）/ [ADR-V6-054](ADR-V6-054.md)~[ADR-V6-060](ADR-V6-060.md)（本批 P1 子项）/ 五份审计 A1-A5 + T6 安全核实

## 背景

ADR-V6-040 统一计划于 2026-07-20 声明「100% 完成」（tip `01208dc5b` 三连 CI 绿，041~052 全交付）。为兑现用户铁律「不要假绿、100% 生产级对外发布」，随后开 **5 个子 agent** 全量审计 fork 代码 + `/Users/wugang/danao14/` 全部文件：

- **A1** fork 反假绿审计
- **A2** fork 铁律+架构合规
- **A3** `/Users/wugang/danao14/` 全文件（V6 设计工作区 + fork 旧快照）
- **A4** fork vs 架构文档对齐
- **A5** 测试/评测/CI 质量
- **T6**（A3 派生）安全项 fork tip `01208dc5b` 核实

## 🔥 诚实口径校准（反假绿核心，本 ADR 的首要价值）

五份审计揭示一个**口径 tension，必须主动澄清**——此前「100% 可分发」的叙事需要校准：

| 范围 | 完成度 | 性质 |
| --- | --- | --- |
| **ADR-V6-040 子集**（041~052） | **100%**（真） | tip `01208dc5b` 三连 CI 绿；**对分发该子集为真** |
| **doc §8 全集** | 缺 3 承重墙 | `time_trend_daily` / `ServiceOrchestrator`+每日批处理 / 音频三表+主权模式 A |
| doc §8 三承重墙 | ADR-040 **明确 defer** | **非假绿**——是显式 scope 裁定，属 Phase 3 |

**校准后口径**：「ADR-V6-040 子集 100% 完成可分发；doc §8 全集尚有 3 承重墙属 Phase 3，已显式 defer 非假绿」。fork 可分发结论不变，但口径现已对齐——这是诚实边界，不是降级。

**五份审计未发现 P0 假绿**：12/13 ADR 核心交付物真落地，唯一「做了没发」（ADR-V6-044 D3 `compute_k_correlations` 生产零可达）属 P1 可闭环，非阻断。

## 去重后的真实问题清单（6 主题 + 3 安全项）

### 代码/工程主题（交叉印证后去重）

| # | 主题 | 印证来源 | 性质 |
| --- | --- | --- | --- |
| T1 | K 域消费层未闭环：`compute_k_correlations` 生产零可达（无 `hermes k` CLI/调度/hook） | A1 P1 + A4 P0 + A3 B4 | 做了没发 |
| T2 | eval-nightly 从未实跑：`DEEPSEEK_API_KEY` secret 未配 → recall gate 理论盾 | A5 P1 | 反假绿基建空转 |
| T3 | windows daemon race 9 处未硬化（`1b2065c11` 只修 1/10） | A5 P1 | CI flaky 源 |
| T4 | theory engine PC/FR per-row 失败只 warn 不入 DLQ；confidence 硬编不接 F6 门 | A2 P1 + A1 P2 | C7/C5 细节收口 |
| T5 | 「写了无消费」碎片：citation counters 无读面、post_gateway_send 未建 | A1 P2 + A4 | 可观测性缺口（P2） |

### 安全项（T6 fork tip 核实后，3 需修 / 3 已闭环）

| # | 项 | T6 VERDICT | 处置 |
| --- | --- | --- | --- |
| T7 | godmode skill 无门禁（`auto_jailbreak` 改 config + 多模型竞速绕审查） | 仍存在，对中文用户现实风险 | **P1 修**（C5/C7） |
| T8 | tool_executor sequential 路径缺单工具超时（挂起子进程永久阻塞 turn） | 中断✓ / 超时❌ | **P1 修**（C7） |
| T9 | `_force_ascii_payload` 永久剥离 CJK（errors='ignore' 删中文，置 True 无 reset） | 仍存在，触发窄但损坏重 | **P1 修**（CJK 完整性） |
| — | K 域向量索引 | 设计正确（K_Correlation 是 relations 边，无 embedding） | ✅ 已闭环 |
| — | 中文化 | 原 "HERMES MESSENGER" 误报（实为 "HERMES AGENT" 品牌画 + 完整 i18n） | ✅ 已闭环 |
| — | 明文 key | 仓库零真实 key，.env 从未进 git，.gitignore 全覆盖 | ✅ 已闭环 |

## 决策：P1 本批（六项，诚实收口）

| 子项 | ADR | 内容 | 风险 |
| --- | --- | --- | --- |
| T3 windows race 9 处硬化 | [054](ADR-V6-054.md) | 裸 `captured[0]` → `_spawns`（已完成 push `50ba42478`） | 低（测试侧） |
| T2 eval-nightly armed 路径 | [055](ADR-V6-055.md) | dormancy `::notice::`→`::warning::` + 7 天 liveness 黄牌 + armed 文档化 | 低（workflow） |
| T1 hermes k CLI 闭环 | [056](ADR-V6-056.md) | `compute_k_correlations` 接 `hermes k` CLI，消「做了没发」 | 中（PTG 层） |
| T4 theory per-row DLQ | [057](ADR-V6-057.md) | PC/FR per-row warn→DLQ（延伸 [052](ADR-V6-052.md)）；confidence 接 F6 门 | 中（PTG 层） |
| T7 godmode 门禁 | [058](ADR-V6-058.md) | SKILL.md 加中文风险提示 + install 前置确认门（参考 obliteratus） | 中（安全） |
| T8 tool_executor 单工具超时 | [059](ADR-V6-059.md) | sequential 路径包 per-tool deadline，超时→DLQ+中断子进程 | 中（agent 层） |
| T9 F9 ascii 收紧 | [060](ADR-V6-060.md) | `errors='replace'` 保留 `?` 占位 + turn 末回滚 + 可见警告 | 中（CJK） |

**T2 armed 说明**：真正 armed 需 `gh secret set DEEPSEEK_API_KEY` + `DEEPSEEK_BASE_URL`，涉及真实 LLM key + nightly 成本 + key 进第三方 CI 安全面——**本 ADR 不擅自配 key**（无值 + 敏感决策），改为「让 dormancy 可见（黄牌+liveness）+ armed 路径文档化」，armed 本身交用户拍板。这是诚实降级，非假绿。

## P2 下批（可观测性 + 措辞校准，本批不做）

- citation counters 读面（`hermes citation stats` 或 doctor 检查）
- F6 公共样本门真接 quark/theory，或改 ADR-V6-042 措辞去 overclaim
- ADR-V6-025 blocklist C4 守卫（1 例钉死 holographic/supermemory 实证）
- 移植 danao14 recall-semantics 评测（17 例 4 维 mutation-guard）作 R8/R9/R12 之外的召回语义门禁

## P3 roadmap（doc §8 承重墙，Phase 3，本批不做）

- `time_trend_daily`（R10 baseline）
- ServiceOrchestrator + 每日批处理流水线（§4.4 整层，K 域 compute 的调度宿主）
- 音频三表（`raw_audio_files`/`audio_clips`）+ 主权模式 A

## 不做（诚实边界）

- **不**在本批做 doc §8 三承重墙（ADR-040 已 defer，Phase 3）
- **不**擅自配 eval-nightly 的真实 key（敏感决策交用户）
- **不**声称「修了产品 bug」当实际只硬化测试（T3，见 [054](ADR-V6-054.md) 反假绿表）
- **不**删 godmode skill（保留安全研究价值 + obliteratus 先例，仅加门禁）

## 反假绿（本 ADR 自身的诚实性）

| 项 | 判定 | 依据 |
| --- | --- | --- |
| 是否主动校准「100%」口径 | ✅ | 本 ADR §诚实口径校准，区分 ADR-040 子集 vs doc §8 全集 |
| 五份审计是否真做 | ✅ | A1-A5 各带 agentId + 证据链；T6 fork tip 逐项 VERDICT |
| P1 六项是否真落地（非声称） | 待证 | 每项独立 ADR + commit + 三连 CI 核验，本 ADR 仅规划锚点 |
| 已闭环项是否核验 | ✅ | T6 对 K向量/中文化/明文key 给 fork tip 证据（非 danao14 旧快照） |

## How to apply

本批 P1 六项**串行 push**（fork CI 有 concurrency group，并发 push 会取消 pending run）：每项独立 commit（ADR + 代码 + C4 测试）→ push → `gh run view --json conclusion` 三连核验（headSha 精确匹配 + conclusion=success + 0 失败）→ 绿后再 push 下一项。收口后写 memory note + 更新 fork 可分发结论。
