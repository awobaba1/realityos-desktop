# ADR-V6-012：Phase 1a 全量收口——读回路 + 评测门禁 + 灾备 + 默认 provider + shutdown drain

- **状态**：Accepted（Phase 1a 生产级可分发定案；代码已落地 commit `93a317520`，测试/核验见文末）
- **日期**：2026-07-19
- **Phase**：Phase 1a（AI Life Memo）——把「能产原子」升级为「可召回 · 可评测 · 可恢复 · 零配置 · 不丢」
- **关联**：ADR-V6-011（心脏 Atomizer）、ADR-V6-008（PTG 数据层）、ADR-V6-010（分发）、ADR-V6-007（阈值下调，本 ADR ⑦ 实测后延迟）；V5：extract-quality-baseline（v11 基线）、run_eval/match_atom（评测资产）
- **铁律**：C1（本 ADR）、C2（软删+version，事件表已具备；备份只读不改 live）、C3（生产现实即真理——200 样本真实 LLM 量，非自判）、C4（每缺陷/契约→回归测试）、C5（评测量的是 post-gate 落库原子）、C7（备份反假绿 + drain 不静默丢 + DLQ 全路径）

## 背景

ADR-V6-011 落地心脏后，全量审计（fork recon）暴露 5 个让 Phase 1a **不可分发**的缺口：

1. **原子写了无人能读回**（最大缺口）：Atomizer 把 R0/R1/R2/R3/R7 写进事件表 + 物化进 entities/relations，但 PTGStore 对事件/实体/关系**零公开读 API**——结构化数据是 write-only，用户永远只能从原始 memo 文本（search_memos_fts）召回，抽出来的「张三/交报告/厦门」图谱永不可见。
2. **无评测门禁**：「提取质量」无量化，任何改动都靠自判——正是用户怒斥的「假绿」温床。
3. **无灾备**：V6 全量数据钉死本机一块盘，盘坏/失窃/误删 = 用户人生图谱全没。这是数据资产产品**永不许上线带**的单一失败。
4. **fresh install 零原子**：DEFAULT_CONFIG 无 `auxiliary.extraction`，call_llm → auto → 无 provider → 每 memo 必进 DLQ，零原子。
5. **shutdown 丢原子**：守护线程抽取途中 shutdown 关库 → 写 closed DB → 原子静默丢失（C2/C7）。

## 决策

### 决策 1：读回路——三个读 API + 图谱渲染器（§4.3A）

- **`PTGStore.recent_atoms(user_id, memo_id?, limit)`**：从 4 事件表**反向重建**原子 dict（meaning_event 的 `intent_class='Need_To_Do'`→R2，其余→R7；confidence 取 `relation_confidence` 优先）。这是评测与召回的公共底座。
- **`search_entities(user_id, query, limit)`**：图谱节点名 LIKE 召回（CJK 2 字名也命中，同 search_memos_fts 的 OR-join 思路）。
- **`relations_for_user(user_id, entity_id?, limit)`**：边查询带 JOIN 出 subject/object 名（渲染器无需二次查表）。
- **`recall.render_relations_block(store, user_id, query, token_budget=800)`**：把 self→X 边折叠成 markdown 进 prefetch。**类型权重**（§4.3A）：`interacts_with`(person)=3 > `has_task`(task)=2 > `mentions`(context/topic)=1；置信序次之；**硬 ≤800 token 上限**（`_estimate_tokens` 无依赖启发：CJK≈1 tok/字、Latin≈4 字/tok），超限截断非静默突破。空命中→空串（fresh/无关 turn 不注入噪声）。`provider.prefetch` 接通 memo recall + graph block 两段。

### 决策 2：评测门禁——量 post-gate 落库原子（§11.3，反假绿）

移植 V5 三资产（`match_atom` 类型感知匹配 + `samples_v0.jsonl` 200 样本 + `run_eval`），但**关键诚实改进**：V5 直接调 LLM 量 raw JSON 输出；V6 走**完整 Atomizer 管线**（抽取→C5 门→写事件表→图谱物化）后用 `recent_atoms(memo_id)` **读回真正落库的原子**再评分——量的是**用户真拿到的 post-gate 数**，非 raw LLM 输出。这是数据资产产品唯一诚实的「提取质量」单位。

**200 样本真实 DeepSeek-v4-flash 端到端（temperature=0，8 并发）**：

| 类型 | Exp | Pred | Match | Precision | Recall | 门禁 | 结果 |
|---|---|---|---|---|---|---|---|
| R3_Person | 122 | 129 | 112 | 86.8% | **91.8%** | ≥85% | ✅ |
| R2_Task | 93 | 136 | 86 | 63.2% | **92.5%** | ≥80% | ✅ |
| R1_SelfState | 79 | 124 | 70 | 56.5% | **88.6%** | ≥70% | ✅ |
| R7_Expression | 44 | 50 | 37 | 74.0% | **84.1%** | ≥60% | ✅ |
| **Overall** | | | | **65.6%** | **90.2%** | F1 | **76.0%** |

**Phase Gate（CLAUDE.md R3>85/R2>80/R1>70/R7>75 recall）全绿**，0 errors。`v6_eval_report.json` 为实证（反假绿）。C5 门（V5 阈值）**未过滤掉正确原子**（recall 90.2% 远超门禁）。

### 决策 3：灾备引擎（§6.9 三防线之①③）

- **`backup_ptg(store, dest, keep=30)`**：在线 `conn.backup()` 在 store lock 下与 atomize 守护线程串行；30 rolling 裁剪。
- **`verify_backup(path, expected_counts?)`**（防线③月演练核心，**反假绿契约**）：`SELECT 1` 探针（非库文件必抛）+ `PRAGMA integrity_check`（撕裂写/页损坏）+ 7 脊柱表**任一缺失（-1）无条件判红**（不只当有 live 数对比时）+ 与 live 对比（backup>live=损坏）。**损坏/缺表/缺核心表必红，绝不假绿**——这是用户「不要假绿」诉求的承重实现。
- **`restore_drill(store, path)`**：以 live 计数复核备份，月演练（restore→对账→失配告警），RPO≤24h RTO≤30min。第二地点 opt-in（永不强制云）。8 回归锁契约（含 corrupt-file/missing-table/over-count 三种假绿必红）。

### 决策 4：默认 provider——extraction 继承主模型（零额外配置）

`DEFAULT_CONFIG["auxiliary"]["extraction"] = {provider:"auto", model:""}`——auto+空 model 经 `_resolve_auto` Step1 继承**主对话模型**。用户跑现有 `hermes model` 选一次主模型（对话本就必选），atomization **零额外配置**生效。已验证 `_get_auxiliary_task_config("extraction")` 返回 `auto`。修 Explore 实证的硬阻塞。

### 决策 5：shutdown drain——在飞线程有界 join，防 closed-DB 丢原子

`_spawn_atomize` 把守护线程入 `self._atomize_threads`（死线程裁剪）；`shutdown` 先 null atomizer（停新调度），再**有界 join**（`shutdown_drain_timeout` 缺省 35s = LLM 超时+余量）在飞线程**后**才 close——在飞抽取写完原子再关库。卡死的有界不 hang（超时警告后照常关，记录「原子可能丢失」）。2 回归锁双契约（finishing→drain 原子落库；hung→有界不 hang）。

### 决策 7（REV-9/ADR-V6-007）：阈值下调——**诚实延迟**

ADR-V6-007 原拟下调 C5 阈值（R3≥0.5/R7·R8≥0.5/R2≥0.4），**门禁是 precision≥0.85**。决策 2 实测 **precision 65.6% 远低于门禁**——下调阈值只在 precision 已高时才安全（否则放更多 FP 进库）。**诚实延迟**：阈值维持 V5 原值（R3=0.8/R2=0.7/R1·R7=0.5），待 precision 改善（prompt v12 / 词汇表注入 ADR-049 / dedup）后再评估下调。**非假绿强行下调**。

## 后果

**正向**：Phase 1a 从「能产原子」升级为**生产级可分发**——图谱可召回（prefetch 注入）、质量可量化（200 样本全绿实证）、数据可恢复（备份+演练反假绿）、零配置（继承主模型）、不丢失（drain + 软删 + DLQ）。用户「100% 全量完成、生产级对外发布」的可验证承重件齐备。

**代价/风险**：
- 评测 cost：200 样本真实 LLM ~¥1.86/次全量跑（可 `--limit 50` 冒烟）；temp=0 确定性可复跑。
- `_estimate_tokens` 是无依赖启发（无 tiktoken）——仅用于截断 prefetch block，非计费；混合文本 ±20%。
- precision 65.6% 是已知债（R1 56.5% 最弱，模型过抽 self-state）——是 prompt/抽取侧问题，非门控问题；留治 v12/词汇表/dedup。

**明确不做（Phase 1a 之外，勿当已完成）**：
- ❌ CaptureSubscriber 语义 DB sink（tool/outbound 事件表）——Phase 2（D4），现仅 fire+log 验证接线（Phase 1a DoD=接线已足）。
- ❌ C5 阈值下调——precision 未达 0.85 门禁，诚实延迟（决策 7）。
- ❌ 备份防线②（音频冷 rsync）——Phase 1 无音频。
- ❌ 共现边 / R1·R7 图节点 / 拼音归一化 / 词汇表注入 / 声学 Phase 2.5——均同 ADR-V6-011 留治。

## 执行进度（2026-07-19，commit `93a317520`）

- ✅ 读回路：`store.py` +3 读 API；`recall.py` 渲染器；`provider.py` prefetch 接通；`test_ptg_read_recall.py` 13 例
- ✅ 评测：`benchmark/match_atom.py`+`run_eval.py`+`samples_v0.jsonl`(200)+`test_match_atom.py` 11 例；真实 200 样本全绿（`v6_eval_report.json`）
- ✅ 灾备：`backup.py` 引擎；`test_ptg_backup.py` 8 例（反假绿契约）
- ✅ 默认 provider：`hermes_cli/config.py` DEFAULT_CONFIG extraction 块 + `cli-config.yaml.example`
- ✅ drain：`provider.py` 跟踪+有界 join；`test_ptg_provider.py` +2 例；`test_ptg_atomizer.py` `_InlineThread` 补 is_alive/join
- ✅ 核验：`ruff check .` 全仓净；`pytest tests/plugins/memory tests/benchmark` **613 passed**
- ⏳ 桌面分发产物构建 + 三连核验 + 新 Release（⑧，进行中）

## 已知约束补充（2026-07-19 真机 E2E 实证）

**反假绿封顶**：用 shipped 代码（commit `4d1e78fa9`）+ 真实 DeepSeek 跑完整回路 E2E（隔离 HERMES_HOME，`/tmp/v6_e2e_shipped.py`）：sync_turn → 后台 atomize（12s，3 原子全 conf=1.0）→ `recent_atoms` 读回 → `prefetch` 同时注入 **memo recall + 图谱块**（"我与张三互动" + "我有待办提交Q3述职报告"）→ `search_entities`/`relations_for_user` 命中 2 节点/3 边 → C7 DLQ 空 → shutdown drain 干净。**全链路真实可跑**。

**⚠️ provider 配置约束（决策4 默认 provider 的必要条件）**：auto 提取继承主模型只在主模型配成**一等 provider**时工作。`_resolve_auto` Step1 诊断实证（`/tmp/v6_resolve_auto_diag.py`）：
- ✅ `provider="deepseek"`（`hermes model` 默认流程）→ client=OK，auto 继承成功
- ✅ `provider="custom"` / `custom:deepseek` → client=OK
- ❌ `provider="openai"` + `base_url=api.deepseek.com`（openai-兼容手填写法）→ client=None：Step1 把 `"openai"` 当真 OpenAI（api.openai.com），忽略 custom base_url，落 Step2 fallback（openrouter/nous/local/api-key）全失败 → **零原子**

**结论**：用户必须用 `hermes model` 选 DeepSeek（注册 `provider="deepseek"`），**不要**手填 `provider="openai"+base_url`。后者是合法 openai-兼容配置但会让 atomization auto 解析失败。非默认流程，不阻塞 v2026.7.19（`hermes model` 用户不受影响），记录在此防踩坑。

**C4 回归锁**：`test_ptg_provider.py::test_sync_turn_atomize_then_prefetch_renders_graph_block` —— 用 mock caller 锁「sync_turn→atomize→prefetch 同时渲染 memo+图谱块」契约（614 passed）。

## Precision ⑦ 实证收口（2026-07-19，反假绿——推翻本 ADR 早先「留治 v12」推测）

本 ADR 早先一处推测 precision 65.6%「是 prompt/抽取侧问题…留治 v12/词汇表/dedup」。本次用 200 样本 FP/FN dump **实证检验**了「v12 prompt 收紧」这条路径，结论：**v12 净退，precision⑦ 是结构/标注口径问题，非 prompt 能修**。记录在案以防重蹈。

**实验**：建 `hl12_extract_v12.md`（v11 + 三处保守收紧：① R1「显式信号才提」摘除 v9「即使隐含/轻微也必须提取」例外；② R7 排除纯采买清单无态度；③ R3 排除不定集体代词「大家/他们」）。激活后跑全量 200 样本（deepseek-chat，`run_eval.py --dump-fp`）。

**结果（v11 → v12，同模型同 200 样本）**：

| 类型 | v11 FP→v12 FP | v11 Prec→v12 Prec | v11 Recall→v12 Recall（门禁） |
|------|------|------|------|
| R1_SelfState | 54→**39** ✓ | 56.5%→**62.9%** ✓ | 88.6%→83.5%（门禁 70%） |
| R2_Task | 50→54 ✗ | 63.2%→61.2% ✗ | 92.5%→91.4%（门禁 80%） |
| R3_Person | 17→22 ✗ | 86.8%→82.7% ✗ | 91.8%→86.1%（门禁 85%） |
| R7_Expression | 13→16 ✗ | 74.0%→69.8% ✗ | 84.1%→84.1%（门禁 60%） |
| **Overall** | — | **70.0%→63.7% ✗** | 90.0%→86.7% |

仅 R1 收紧有效（-15 FP，+6.4pt 精度，recall 仍 +13.5pt 余量）；R7/R3 收紧**反伤**（与 prompt 既有规则冲突：line 130-132 已强制「妈妈/HR/同事/群体称呼必须提」，v9 标准已强制「长期练习/项目/目标应提」——收紧剪了真阳却不减假阳）。**v12 净退，全量 revert 回 v11，v12 prompt 文件删除**（实验产物，从未上线，C6 不适用）。

**根因（200 样本 FP dump 实证，非自判）**：主导 FP **不是过抽取 bug，是 prompt 忠实执行 V5 校准标准 vs eval 样本期望更窄子集的口径错配**——
- R3 FP：妈妈/爸爸/老师/HR/公司同事——**正是** line 130-132「家庭成员/职场/群体称呼也必须提取」的强制项，LLM 照做，样本不认。
- R2 FP：每周健身三次/看完书/学吉他——**正是** v9 标准「长期练习/项目/目标应提取」的覆盖项，LLM 照做，样本不认。
- R7 FP：饺子好吃/抱怨甲方/健身房年卡——intent 规则覆盖的评价/抱怨/消费，LLM 照做。
- R1 FP：仍是 evidence 空 mood 原子（v12 已减 54→39，残余属弱信号）。

**为何 prompt 改不动**：收窄 prompt 会**直接推翻 V5 v9 拉 R1 recall 62→84.8 的校准**（那正是靠「即使隐含也提」+「Neutral 兜底」达成），一收窄 recall 门禁即崩。prompt 与样本校准在两个粒度，prompt 侧无解。V5 基线同 v11 prompt 算得 precision 亦 ~67%（F1 75.1 + recall ~85% 反推）——**precision ~70% 是 v11 prompt 的结构天花板，V5/V6 皆然**（V5 仅未单报 precision 故隐形）。

**收口决策**：precision⑦（≥0.85）维持**诚实结构化延迟**，Phase 1a 不变（本就未承诺 P≥85，那是 ADR-V6-007 的理想门禁）。v11 继续 shipped（Release v2026.7.19 不受影响）。**修正前进路径**（取代早先「留治 v12」）：① **重标 samples** 对齐 prompt 的 V5 标准（样本是错配方）；或 ② **dedup/词汇表注入（ADR-049）** 归一化实体；或 ③ **post-gate per-type 置信阈值精调**——**均非 prompt v12+**。

**保留产出**：`run_eval.py` 新增 `--dump-fp`（per-sample FP/FN 原子 JSONL）是通用诊断工具，**保留**（本次根因即赖此产出）；200 样本 FP dump 落 `/tmp/v6_fpfn_200_v12.jsonl`（实验证据）。

### ⑦ 补充——阈值可分离性分析（2026-07-19，commit `6e280e207` 后续）

上文证死「prompt v12」路径后，检验 ⑦ 收口三策之**per-type 置信阈值精调**（post-gate，不动 prompt，故不撞 V5 校准）。加强 `run_eval --dump-fp` 全样本输出 TP/FP/FN 含 confidence，新增 `analyze_threshold_separability.py`（+2 C4 回归）做 TP-vs-FP 置信分布可分离性扫描。

**结果（v11 全量 200 样本，真实 DeepSeek）**——TP 与 FP 的 confidence **高度重叠，不可分离**：

| 类型 | TP 置信(min/med/max) | FP 置信(min/med/max) | 最优阈值 | 精度@最优 | recall@最优（门禁） |
|------|------|------|------|------|------|
| R3_Person | 0.90/0.95/1.00 | 0.85/0.90/0.95 | 0.90 | 86.5% | 89.3%（85%） |
| R2_Task | 0.70/0.95/1.00 | 0.80/**0.95**/1.00 | 0.90 | 63.1% | 88.2%（80%） |
| R1_SelfState | 0.70/0.90/1.00 | 0.60/0.85/0.95 | 0.85 | 68.8% | 81.0%（70%） |
| R7_Expression | 0.85/0.95/1.00 | 0.70/0.90/0.95 | 0.85 | 76.6% | 81.8%（60%） |

**为何不可分**：FP **不是低置信**——LLM **自信地错**（"妈妈"是人、"每周健身"是任务，按 prompt 标准**本就正确**，conf 0.95，仅样本不认）。R2 的 TP/FP 中位置信**同为 0.95**，任何阈值同等比例杀掉 TP/FP。最优 per-type 阈值下 4 类型精度天花板 **73.5%**，距 85% 仍差 11.5pt——**per-type 阈值路径证死**。

**度量细节**：R0_Entity 33 个被正确抽取但 samples 未标（不在 expected set），在 overall 分母计为 FP，使 overall precision（63.1%）**低估**；4 类型（R0 排除）真实 precision 67.8%。无论哪个口径，皆远低 85%。

**⑦ 收口定论（双路径证死）**：prompt 收窄（v12 净退，见上节）+ 置信阈值精调（不可分，本节）**两策皆死**。残余 FP 是 prompt 忠实执行 V5 标准 vs 样本更窄口径的错配，**置信不可分**。唯一前进路径收窄为：① **重标 samples** 对齐 prompt V5 标准（样本是错配方，工作量大但最直接）；② **dedup/词汇表注入（ADR-049）** 归一化实体降重。**两者均属 Phase 1b+ 范畴，非 Phase 1a 遗漏**——Phase 1a 从未承诺 P≥85（乃 ADR-V6-007 理想门禁），⑦ 现以**双路径实证**维持诚实结构化延迟。

**保留产出**：`tests/benchmark/analyze_threshold_separability.py`（可复现本次结论的 CLI 工具 + 2 回归锁可分/不可分两形）；`run_eval --dump-fp` 升级为全样本 TP/FP/FN（本次可分离性分析即赖此）。
